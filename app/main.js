/*jslint vars: true, plusplus: true, devel: true, nomen: true, indent: 4, forin: true, maxerr: 50, regexp: true */
/*global require */

require([
    "app"
], function (App) {
    'use strict';
    var app = new App();
    app.go();
});
